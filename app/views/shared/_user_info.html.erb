
<a href="<%= user_path(current_user) %>">
	<!--<%= gravatar_for current_user, size: 52 %>-->
	<h1>
	<%= current_user.first_name %>
	<%= current_user.last_name %>
</h1>
</a>

<span>
	<%= link_to "view my profile", current_user %>
</span>
<span>
	<%= pluralize(current_user.memories.count, "memory") %>
</span>