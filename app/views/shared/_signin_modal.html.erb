<!-- Modal -->
<div id="signin_modal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="signin_modalLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="signin_modalLabel">Log in to FamilyTales</h3>
  </div>
  <div class="modal-body center">
  <div class="social-signin">
   <p><a class="btn-auth btn-facebook large" href="/auth/facebook">Login with <b>Facebook</b></a> </p>
  </div>
  <br>
    <%= form_for(:session, url: sessions_path) do |f| %>
    <p>
      <%= f.text_field :email, :placeholder=>"Email address" %>
      <br>
      <%= f.password_field :password, :placeholder=>"Password" %>
    </p>
    
  </div>
  <div class="modal-footer" syle="text-align: center;">
      <%= f.submit "Log in", class: "signin_button"%><br>
        <% end %>
    <div class="forgot_password"><%= link_to "Forgot your password?", new_password_reset_path %><br>
    <a href="#contact" data-dismiss="modal" aria-hidden="true">Sign up now</a>
    </div>
    </p>
  </div>
</div>