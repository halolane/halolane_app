
  <div class="navbar-inner">
    <div class="container">
      <nav>
        <ul class="nav btn-topbar-left" style="left:0;">
          
          <li class="sneakpeekheader-text">Here is a sneak peek of <strong style="font-weight:bold;">Alex Dunphy</strong>'s storybook. You're logged in as <strong style="font-weight:bold;">Explorer</strong>.</li>
        </ul>
        <ul class="nav pull-right">
          <% if signed_in? && current_user.email == ('familytalesuser2@gmail.com') and @profile.url == "alexdunphy" %>
            <li class="signupbutton">
              <a class="green_button signup_button" href="/signup">Start your own storybook</a>
            </li>
          <% else %>
            <li class="signupbutton">
              <a class="green_button signup_button" href="/">Start your own storybook</a>
            </li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
