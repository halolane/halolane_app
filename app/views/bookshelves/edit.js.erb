$(document).ready(function(){
	<%= remotipart_response do %>
		$(".edit_bookshelf").bind("ajax:success", function() { 
	    $(this).closest('.bookshelfName').html("<%= escape_javascript render('bookshelves/form') %>");
	  });
	<% end %>
});