$(document).ready(function(){
	<%= remotipart_response do %>
		$(".likebutton").live('ajax:success', function(evt, data, status, xhr){
	    $(this).closest('div').html("<%= escape_javascript render('shared/like_form') %>");
	  });
	<% end %>
});